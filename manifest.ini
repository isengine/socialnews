{
    // system
    // для определения совместимости с версией системы
    "system" : "",

    // libraries
    // для определения наличия нужных библиотек в системе,
    // без которых модуль не будет работать
    // ПОКА НЕ РЕАЛИЗОВАНО
    "libraries" : [
    ],

    // settings index
    // триггер типа чтения настроек - как индексы или нет
    "settings-index" : null,

    // settings
    // базовые настройки
    "settings" : {
    },

    // data index
    // триггер типа чтения настроек - как индексы или нет
    "data-index" : null,

    // data
    // настройки для итерируемых данных, задаются по ключу для этих данных
    // ПОКА НЕ РЕАЛИЗОВАНО
    "data" : {
        
    }
}
